# OFFLINE lane, the engines this product actually runs. Every entry is documented in docs/frameworks/ and
# used by a stage under data-pipeline/. Pins are added by the unit that first runs the engine, so this file
# never advertises a dependency the code does not use.

# U1, connectome construction: the release ships Apache Arrow Feather tables, and the column assignment and
# filter accumulation are array work.
pyarrow==25.0.1
numpy==2.5.3

# U2, the network engine: the connectome-constrained network, its simulator and its training loop.
# torch is pinned without a build tag, so the CPU wheel (CI) and the CUDA wheel (requirements-gpu.txt) both
# satisfy it; install the one for the machine first.
torch==2.14.0
torchvision==0.29.0
flyvis==1.2.0
# The engine's storage layer. Its last release (1.0.0) unlinks an HDF5 file while its own handle is still
# open, which works on Linux and fails on Windows the first time a connectome is compiled; the fix is on the
# upstream main branch (commit 3b9792c, 2026-02-27) and unreleased, so it is pinned by commit.
datamate @ git+https://github.com/flyvis/datamate@3b9792c3c90fb29d741f8185c7aca912aa0c0942

# U4, vision data: member-level fetching over HTTP ranges (requests), the source configuration (PyYAML), and
# TartanAir V2 decoding through OpenCV, as its reference reader does it (the channel order of its packed
# depth and flow PNGs is OpenCV's; a PIL decode swaps flow with its mask).
requests==2.34.2
PyYAML==6.0.3
opencv-python-headless==5.0.0.93
