# Frameworks

One card per engine this product actually runs: what it is, why it was chosen, the exact pin, how it is used
here, and the license of its code and of its weights. A card appears when its engine lands.

Planned cards, in the order their units arrive: flyvis (connectome-constrained networks), neuprint-python
and navis (connectome access), PyTorch, OpenCV, scikit-image, FlyGym, the depth and segmentation engines of
the method ladder, and onnxruntime-web for the browser lane.
