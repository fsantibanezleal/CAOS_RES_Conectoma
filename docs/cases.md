# Cases

The case registry is the validation design of this product: nominal indoor and outdoor motion, lighting and
acquisition extremes, transfer domains, ethological scenes rendered in fly-eye space, and explicit controls
including inputs where depth is unobservable by construction.

The taxonomy, the coverage matrix and one page per case are written by the unit that builds the registry.
