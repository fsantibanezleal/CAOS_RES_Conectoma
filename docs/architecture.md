# Architecture

- [01, overview](architecture/01_overview.md): the lanes, the flow, what is frozen and what is learned
- [08, the two data contracts](architecture/08_data-contracts.md): connectome and vision ingestion, and the
  artifact contract the web consumes

Pages for determinism, the live gate, the staged pipeline, evaluation and deploy are written by the units
that build those parts, so a page here always describes something that exists.
